# Database
DATABASE_URL=file:./prisma/spelklar.db

# JWT Secret – Change this to a random string in production
JWT_SECRET=your-secret-key-here-change-in-production

# Demo Mode - Set to "true" to accept any 6-digit OTP code (useful for testing)
# Automatically enabled if: NODE_ENV=development OR SMS credentials not set
DEMO_MODE=true

# 46elks SMS API credentials (optional in development)
# Get these from https://46elks.com/dashboard
#
# IMPORTANT: In DEMO MODE (DEMO_MODE=true, NODE_ENV=development, OR missing SMS credentials),
# the app accepts ANY 6-digit OTP code!
# This makes testing super easy - just type any numbers like 123456 or 999999
#
# To enable real SMS (production):
# 1. Set SMS_USERNAME and SMS_PASSWORD
# 2. Set DEMO_MODE=false
# 3. Users will only be able to verify with the exact generated code
SMS_USERNAME=your-46elks-api-username
SMS_PASSWORD=your-46elks-api-password

# Cloudflare R2 (optional in development)
# Get these from your Cloudflare dashboard
# Without R2, photos will be stored in demo mode (local only)
R2_ACCOUNT_ID=your-account-id
R2_ACCESS_KEY_ID=your-access-key
R2_SECRET_ACCESS_KEY=your-secret-key
R2_BUCKET_NAME=spelklar-photos

# Environment
NODE_ENV=development

# Frontend URL for CORS (set on Render)
FRONTEND_URL=https://your-vercel-domain.vercel.app

# Server port
PORT=3001
